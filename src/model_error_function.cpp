#include "pch.h"
#include "model_error_function.h"


double literature_fitness_function(std::function<double(string&)>& func)
{
	// Assume, that author data is already loaded!
	vector<double> one_author_dists, different_author_dists;

	for()

	return 
}
